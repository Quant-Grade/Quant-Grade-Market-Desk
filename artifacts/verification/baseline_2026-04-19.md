# Baseline Integrity — 2026-04-19

**Purpose:** S0 read-only proof capture before any S1+ change.
**Repo HEAD at capture:** `28c8d6b` (milestone: add Scribe v0 ledger with stable LF hash proof).
**Pre-existing uncommitted changes (not touched in S0):** `M .gitignore`, `M rag_system_v2/src/doctor.py`, `M rag_system_v2/src/retrieve.py`, `M rag_system_v2/tests/test_smoke.py`, plus prior `?? artifacts/verification/_resume_*` and `?? artifacts/verification/governance_foundation_proof_a.py` and `?? Recap.md`.
**Python:** 3.12.10 (`C:\Users\M.R Bear\AppData\Local\Programs\Python\Python312\python.exe`).
**Mode:** read-only (one bounded live round was deliberately not executed; Wave 1 P3 already exercises the loop's baton path, and this S0 ended at the decision gate).

---

## Decision-Changing Proofs

### Wave P1 — atomic commit + rollback
- Command: `python -X utf8 tools\verify_p1_atomic_commit.py`
- Exit: **0**
- stdout (verbatim, full):
  ```
  [SCRIBE] 17:49:09 Ledger append failed
     'C:\\Users\\M.R Bear\\AppData\\Local\\Temp\\tmp6vgdlp_w\\data\\alpha_concepts.jsonl' is not in the subpath of 'C:\\Users\\M.R Bear\\Desktop\\backend_tribunal\\RAG_SYSTEM'
  commit_ok
  rollback_ok
  ```
- **Status: GREEN with named anomaly.** The atomic commit + rollback contract is intact (`commit_ok` + `rollback_ok`). However Scribe emitted `[SCRIBE] Ledger append failed` because `_append_scribe_ledger` calls `alpha_path.resolve().relative_to(_script_dir.resolve())`, which raises a `ValueError` when the harness's `tempfile.TemporaryDirectory()` is outside the repo root. Scribe's design ("never raises") catches and logs, so functional behavior holds — but cross-tree harness invocations cannot exercise the Scribe ledger append path. **This is a new finding not in any prior audit.**

### Wave 1 — P6 + P2 + P3
- Command: `python -X utf8 tools\verify_wave1.py`
- Exit: **0**
- stdout: `p6_missing_ok / p6_allow_ok / p2_ok / p3_ok / wave1_all_ok`
- **Status: GREEN.** P6 bounds gate, P2 resume, P3 baton all proven against today's binaries.

### Wave 2 — P5 + P8 — **RED (decision-changing failure)**
- Command: `python -X utf8 tools\verify_wave2.py`
- Exit: **1**
- Failure: `AssertionError` at `verify_wave2.py:14` — `assert txt.startswith("### STATE OF THE THEORY (Round 7)")`.
- **Root cause: harness drift, not orchestrator regression.** Round 85 (`fix_plan.md` Round 85) migrated `prepend_state_summary` away from rewriting `idea_log.md` with a `### STATE OF THE THEORY` prepend, to **append-only** `theory_log.md` with `theory_log_version: 1` YAML frontmatter. The orchestrator implements the new contract; `verify_wave2.py` asserts the old contract. This harness has not been updated since Round 85. The grep evidence confirms the harness even references `idea_log.md.tmp` (`verify_wave2.py:16: assert not (td / "idea_log.md.tmp").exists()`), which was the pre-Round-85 atomic-rewrite scratch file.
- **Implication for the plan:** Wave 2's regression net for P5 and P8 is non-functional today. Wave 2 must be re-aligned to Round-85 behavior before being trusted as a gate. This is a meta-instance of the α-coupling problem the prior audits named.

### Wave 3 — P4 + P7
- Command: `python -X utf8 tools\verify_wave3.py`
- Exit: **0**
- stdout: `p4_order_ok / p7_strict_fail_ok / p7_strict_ok_ok / p7_prose_escape_ok / p7_loose_temp_ok / wave3_all_ok`
- **Status: GREEN.** Round-flow ordering and strict-JSON Leader (including repair pass and prose-fallback escape) all proven.

### Proof A — governance foundation
- Command: `python -X utf8 artifacts\verification\governance_foundation_proof_a.py`
- Exit: **0**
- stdout: `PROOF_A_OK score_range option_count baton_mismatch_control checkpoint`
- **Status: GREEN.** Governance schema, score range, option count, baton-mismatch sync control, and checkpoint capture all hold.

### Doctor — `python -X utf8 -m src.doctor` (cwd: `rag_system_v2/`)
- Exit: **1**
- Overall: **UNHEALTHY** (8 pass, 0 warn, 2 fail).
- Pass: Files exist; manifest valid (v2, 2296 chunks); alpha_concepts_chunks (0, valid); parents (541, 8 docs); Qdrant (2296 vectors, dim=384); ID consistency (2296 IDs); query latency (19 ms); LM Studio (connected, **11 models available**).
- **Fail #1: chunks.jsonl** — "2296 issues found in chunks.jsonl". Every chunk fails some doctor validator. Specific failure mode not surfaced by the summary line; needs follow-up to characterize before manifest rebuild.
- **Fail #2: BM25 index** — `Failed to load BM25 index: 'dict' object has no attribute 'get_stats'`. API drift between BM25 index pickle and current `index_bm25.BM25Index.load()`/`get_stats()` interface.
- **Implication for S2 (manifest v3):** manifest v3 rebuild without addressing the chunks-validation failure and BM25 API drift will produce a v3 manifest that doctor still flags unhealthy. The manifest rebuild step alone cannot turn doctor green — these are real issues independent of path staleness. **Decision-changing.**
- **Note:** manifest is reported as v2 — confirms S2 hasn't run; matches expectation.

### Smoke suite — `python -X utf8 -m pytest rag_system_v2\tests\test_smoke.py -v`
- Exit: **1**
- Result: **2 passed / 8 failed** (unchanged from `fix_plan.md` Round 79).
- Two passes: `test_hash_consistency`, `test_bm25_tokenization_stability`.
- Eight failures, classified per-cluster:
  | # | Test | Failure | Classification |
  |---|---|---|---|
  | 1 | `test_chunk_id_determinism` | "Chunk ID should have separators" — current chunk-ID format lacks `:` or `_` | src drifted (chunk-ID format changed); decision pending — accept new format or restore separator |
  | 2 | `test_router_decision_mapping` | "Missing tier: FAST" — `ModelTier.FAST` enum present in `config.py` but mapping in router doesn't expose it where the test expects | test drifted to old API |
  | 3 | `test_verify_fail_closed` | `ImportError: attempted relative import with no known parent package` (in `verify.py`) | test invokes module as top-level; `from .config` fails |
  | 4 | `test_config_validation` | `RouterConfig.refuse_threshold` does not exist; current attr is `t_refuse_threshold` | test drifted; accept new naming |
  | 5 | `test_manifest_schema_version` | `ImportError: attempted relative import` (in `build_all.py`) | same import-style issue as #3 |
  | 6 | `test_embedding_config_consistency` | `EmbeddingConfig.model_name` does not exist; current attr is `model` | test drifted; accept new naming |
  | 7 | `test_injection_detection_exists` | `ImportError: attempted relative import` (in `router.py`) | same import-style issue as #3, #5 |
  | 8 | `test_rrf_merge_correctness` | `'list' object has no attribute 'chunk_id'` — RRF merge return shape changed | src drifted; need decision |

  Three failures (#3, #5, #7) are the **same bug**: the smoke test imports modules as top-level scripts and the modules use `from .config import …` which requires package context. This is a single fix (test import discipline) covering 3/8 failures. Five remaining are attribute/format drift requiring per-cluster classification.
- **Implication for S4:** smoke alignment scope is concrete now — 1 cluster of 3 import-discipline failures, plus 5 individually-classified drift items. Per `REGRESSION_NET.md` discipline (S4a), no smoke PR may touch `src/` without explicit "test drifted vs src drifted" classification. Three drifts are clearly test-drifted (#2, #4, #6 — attribute renames where current `src` names are more disciplined). Two need investigation (#1, #8 — chunk-ID format and RRF return shape; could be intentional API improvements or unintended regressions).

### parents.sqlite ↔ chunks.jsonl consistency
- Result: **GREEN.**
  - `parents` table count: 541
  - distinct `parent_id` in `chunks.jsonl`: 541
  - chunks with orphan `parent_id`: **0**
  - chunks total: 2296 (matches manifest)
- Blind spot **B1 dismissed.** Parent expansion has full referential integrity.

### Targeted greps
- **B6 (`input()` reachability):** INITIALLY RAISED, CORRECTED TO DISMISSED (post-S0 follow-up grep). `orchestrator.py:314: ip = input().strip()` lives inside `ensure_builder_ip()` (lines 289–332). A repository-wide grep for `ensure_builder_ip` returns exactly two hits outside the definition itself: this baseline artifact (which contained the now-corrected false claim), and `orchestrator.py.pre_hardening` (a historical backup, not live code). The function has **zero call sites in `main()` or anywhere else**; the `input()` cannot fire. No guard is needed because the branch is unreachable. The dead function is a separate α-surface cleanup decision outside S0 scope. Note: `EOFError` is already caught at line 315 and the fail-closed `sys.exit(1)` path at line 319 would fire cleanly even if the function were reachable — the original concern about "hangs forever" does not match the actual code.
- **B10 (`send_webhook_recap` timeout):** DISMISSED. `orchestrator.py:1616: with urllib.request.urlopen(req, timeout=10) as resp:` — explicit 10-second timeout. Also the model-fetch path at line 343 uses an explicit `timeout` parameter. **No S1b guard needed for B10.**
- **`idea_log.md.tmp` references:** RAISED with five hits.
  - `fix_plan.md:1251` — historical narrative, OK.
  - `MASTER_PLAN.md:39` — *current* doc still describing the **pre-Round-85** P5 behavior (`prepend_state_summary writes idea_log.md.tmp then os.replace`). **Stale doc.**
  - `COPILOT.md:41` and `COPILOT.md:91` — *current* doc, same stale claim.
  - `tools/verify_wave2.py:16` — the harness asserting the now-incorrect post-write cleanup. **This is the same code that produced the Wave 2 RED.**
- **Implication for S1a:** the planned `idea_log.md.tmp` deletion is **only safe after** `MASTER_PLAN.md`, `COPILOT.md`, and `tools/verify_wave2.py` are updated to drop their references. `idea_log.md.tmp` itself on disk (the 1.1 MB file) is referenced in code/docs that still claim the old behavior. **Cannot delete in S1a as planned without first re-aligning Wave 2.**

### Scribe hash self-check
- Last `alpha_concepts.jsonl` line SHA-256 (over bytes-as-written + `\n`): `423ae1f570089bfb50140fca87f569a1f52279cffe9fb1d0a2bb5448fc801d94`
- Last `scribe_ledger.jsonl` entry's `checkpoint_sha256`: `423ae1f570089bfb50140fca87f569a1f52279cffe9fb1d0a2bb5448fc801d94`
- **Last-pair match: TRUE.**
- Cross-pair check: Scribe has 2 lines, both `round_id=1`. The first Scribe entry's `checkpoint_sha256` does NOT match any current `alpha_concepts.jsonl` line (out of 105 lines). The second Scribe entry matches a current line.
- **Interpretation:** the first Scribe entry is **orphaned** — its referenced checkpoint line is no longer present in `alpha_concepts.jsonl`. This is direct on-disk evidence of the cold-start `round_id` duplication footgun (fragility #4 from prior audits): a cold start without `ALPHA_RESUME=1` writes a new `round_id=1` line that supersedes the old one, but the old line's Scribe entry remains pointing at bytes no longer present.
- **Decision impact:** Scribe v0's per-entry hash design (no `prev_hash` chain) means this orphan does not propagate. New entries continue to verify cleanly. The forward-looking byte-stability gate for S3 is unaffected. But this is the first **direct evidence** of cold-start orphan in the Scribe chain, captured in this S0 pass. **Worth recording; does not block S1.**

---

## Bounded live round
**Deliberately not executed.** Reasons:
1. S0 is framed read-only; a bounded round mutates four files (`alpha_concepts.jsonl`, `idea_log.md`, `scribe_ledger.jsonl`, possibly `theory_log.md` if at a 5-round boundary).
2. The decision gate has already tripped (Wave 2 RED, doctor UNHEALTHY); no additional evidence is needed to support the stop.
3. Wave 1's P3 test exercises the orchestrator loop's baton path against the current binary.
4. Mutating state in the same step that triggers the stop event would muddy attribution if anything went wrong post-report.

LM Studio is reachable with 11 models available, so a bounded round is technically possible if needed later under explicit operator approval.

---

## Decision Gate Outcome

**S0 verdict: NOT GREEN ENOUGH for S1 to proceed without explicit operator decision.**

Failing decision-changing items:
1. **Wave 2 harness drift.** Regression net for P5 + P8 is non-functional. The orchestrator is correct (Round 85 contract); the harness is stale.
2. **Doctor unhealthy.** Two real failures (chunks validation 2296/2296 issues; BM25 API drift) independent of manifest-path staleness — they will not be resolved by a manifest v3 rebuild alone.
3. ~~**B6 guard needed.**~~ **B6 corrected to DISMISSED** — post-S0 grep proved `ensure_builder_ip` has zero call sites; `input()` at line 314 is unreachable. No guard required. (Original false-positive retained for traceability above.)
4. **`idea_log.md.tmp` retirement is blocked** by stale references in `MASTER_PLAN.md`, `COPILOT.md`, and `verify_wave2.py`. The retirement plan in S1a as originally scoped cannot land cleanly until those references are first updated.

Passing decision-changing items:
- P1, Wave 1, Wave 3, Proof A all GREEN.
- parents.sqlite fully consistent.
- B10 webhook has timeout (no guard needed).
- LM Studio reachable.

**Per execution discipline ("If S0 is not green enough, do not continue"), execution stops here. No S1 changes have been made. No load-bearing files have been touched.**
